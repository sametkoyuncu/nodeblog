<div class="container">
    {{#if sessionFlash.message}}
    <div class="{{sessionFlash.type}} alert-dismissible fade show" role="alert">
        {{sessionFlash.message}}
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    {{/if}}
    <div class="col-md-12">
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Blog Listesi</h6>
            </div>
            <div class="card-body p-0">
                <table class="table table-striped">
                    <thead class="bg-gradient-primary text-light">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Başlık</th>
                            <th scope="col" class="text-center">Tarih</th>
                            <th scope="col" class="text-center">Ayarlar</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each posts}}
                        <tr>
                            <th scope="row">#</th>
                            <td>{{title}}</td>
                            <td class="text-center">{{generateDate date 'DD MMM YYYY'}}</td>
                            <td class="text-center">
                                <button type="button" class="btn btn-sm btn-info" title="İncele">👁️</button>
                                <button type="button" class="btn btn-sm btn-success" title="Düzenle">✏️</button>
                                <button type="button" class="btn btn-sm btn-danger" title="Sil">❌</button>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>